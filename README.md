## Euro Multi-Agent Voice Trip Planner

This repo contains:
- **Backend**: `backend/` (FastAPI + multi-agent planner)
- **Frontend**: `frontend/` (Next.js voice + chat UI)

For the full setup/deployment guide, see:
- `frontend/README.md`

